<!--components/listview/listview.wxml-->
<view class="container">
  <view class="list">
    <listitem wx:for="{{showList}}" wx:key="unique" item-prop="{{item}}"></listitem>
  </view>
  <pagination page-size="{{pageSize}}" totalPage="{{totalPage}}" bind:change-page="onPageChanged"/>
</view>